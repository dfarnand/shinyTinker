# shinyTinker Examples

`demo.R` is a shiny application which shows the usage of the custom widgets in this package.

## Running the Demo

The app should load the package from the current directory automatically, just run

```r
shiny::runApp("inst/examples/demo.R")
```
