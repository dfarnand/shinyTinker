% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/navDownloadButton.R
\name{navDownloadButton}
\alias{navDownloadButton}
\title{Create a navbar download button for bs4Dash (AdminLTE 3)}
\usage{
navDownloadButton(outputId, tooltip = "Download CSV", icon_name = "download")
}
\arguments{
\item{outputId}{The output ID matching a \code{downloadHandler()} in the server.}

\item{tooltip}{Label shown on hover. Pass \code{NULL} to suppress.}

\item{icon_name}{Font Awesome icon name passed to \code{shiny::icon()}.}
}
\value{
A \code{shiny.tag} suitable for \code{rightUi}/\code{leftUi} in
\code{bs4Dash::dashboardHeader()}.
}
\description{
Renders a single icon link in the AdminLTE navbar that triggers a Shiny
download handler directly — no dropdown required.
}
\details{
The \verb{<li class="nav-item dropdown">} wrapper satisfies bs4Dash's
\code{dashboardHeader()} assertion on \code{rightUi} children. The inner \verb{<a>} carries
\code{shiny-download-link} so Shiny's \code{DownloadLinkOutputBinding} picks it up
without any custom JS.
}
\examples{
\dontrun{
library(shiny)
library(bs4Dash)
ui <- dashboardPage(
  header = dashboardHeader(
    title = "Demo",
    rightUi = navDownloadButton("download_data", tooltip = "Download CSV")
  ),
  sidebar = dashboardSidebar(),
  body = dashboardBody()
)
server <- function(input, output) {
  output$download_data <- downloadHandler(
    filename = "data.csv",
    content = function(file) write.csv(mtcars, file)
  )
}
shinyApp(ui, server)
}
}
